# Leave unset for local dev — Vite's dev server proxies /api to localhost:4000.
# In production (Vercel), set this to your deployed backend's base URL:
# VITE_API_URL=https://citywish-backend.onrender.com/api
VITE_API_URL=
